# BK Admin 整体测试计划

**日期：** 2026-04-15
**状态：** 待执行
**前提：** 需要 3 个账号（admin / approver / viewer）

---

## 一、角色权限测试

| # | 测试项 | admin | approver | viewer | 验证方法 |
|---|--------|-------|----------|--------|---------|
| 1.1 | 登录后默认跳转 | 仪表盘 `/` | 我的工作台 `/my-dashboard` | 我的工作台 `/my-dashboard` | 登录观察 URL |
| 1.2 | 左侧菜单可见性 | 仪表盘+全部菜单 | 工作台+发布队列+配置 | 工作台+发布队列 | 检查菜单项 |
| 1.3 | 我的工作台入口 | 不显示 | 显示 | 显示 | 菜单检查 |
| 1.4 | 仪表盘入口 | 显示 | 不显示 | 不显示 | 菜单检查 |
| 1.5 | 人设管理"新增"按钮 | 显示 | 不显示 | 无此页面 | 按钮可见性 |
| 1.6 | 系统管理菜单 | 显示（配置/采集源/审计/用户） | 不显示 | 不显示 | 菜单检查 |
| 1.7 | URL 直接访问受限页面 | 正常 | 重定向 | 重定向 | 浏览器输入 URL |

## 二、我的工作台模块（approver 专属）

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 2.1 | 页面加载 loading | 数据加载完成前显示遮罩，按钮不可点 |
| 2.2 | CTA 卡片显示 | 待审池数量 + 已认领数量 + 开始审核按钮 |
| 2.3 | 点击开始审核 | 认领 10 条，进入聚焦视图 1/N |
| 2.4 | 工作台进度显示 | `工作台 (1 / 10)` + TTL 倒计时 |
| 2.5 | 通过操作 (J) | 状态变 approved，保留在工作台，按钮变"发布" |
| 2.6 | 拒绝操作 (K) | 弹出备注框，确认后移除，进入下一条 |
| 2.7 | 跳过操作 (S) | 释放回池，进入下一条 |
| 2.8 | 发布操作 | 调用 BK API 发布，成功后移除 |
| 2.9 | 通过并发布 | 先 approve 再 post，一步完成 |
| 2.10 | 发布失败处理 | 显示 failReason，出现"重新通过"按钮 |
| 2.11 | 编辑按钮 | 打开编辑弹窗，保存后刷新内容 |
| 2.12 | 重新生成按钮 | 调用 regenerate API，内容更新 |
| 2.13 | 结束审核 | 释放未处理 feeds 回池，刷新统计 |
| 2.14 | 批次完成 | 显示"批次完成"+ 统计 + "抽取下一批"按钮 |
| 2.15 | 页面恢复 | 刷新页面，自动恢复未完成工作台 |
| 2.16 | 右上角统计实时更新 | 每次操作后数量变化 |
| 2.17 | 按钮 loading 互斥 | 操作中其他按钮 disabled |
| 2.18 | 键盘快捷键 | J/K/S/O 功能正常 |

## 三、发布队列模块（feeds 页面）

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 3.1 | Tab 切换 | pending/approved/posted/rejected/failed 正常切换 |
| 3.2 | Tab 数量徽标 | 每个 tab 显示正确数量 |
| 3.3 | 审阅者数据过滤 | pending 按 claimedBy，其他按 reviewedBy |
| 3.4 | 管理员看全部 | 不受过滤限制 |
| 3.5 | 分页 | 翻页正常，不重置为第1页 |
| 3.6 | 内容显示 | finalContent \|\| draftContent，不截断 |
| 3.7 | 人设编辑图标 | 右上角图标，新窗口打开对应人设编辑 |
| 3.8 | failed 状态 | 显示 failReason + "重新通过"按钮 |
| 3.9 | rejected 状态 | 显示"撤回待审"按钮，无编辑/重新生成 |
| 3.10 | posted 状态 | 无编辑/重新生成/通过/拒绝按钮 |
| 3.11 | 立即发布 | 同步调用 BK API，成功提示"发布成功" |
| 3.12 | 来源过滤 | scanner/trends/custom 标签过滤 |
| 3.13 | 版块过滤 | 下拉选择版块过滤 |
| 3.14 | 左下角 tag 跳转 | 点击通过/拒绝/已发布/跳过跳转对应 tab |

## 四、编辑弹窗

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 4.1 | 源帖文显示 | threadContent 有内容显示，为空不显示 |
| 4.2 | 编辑框内容 | 填入 finalContent \|\| draftContent |
| 4.3 | 重新生成按钮 | 调用 API，编辑框内容更新 |
| 4.4 | 保存 | 保存到 finalContent |
| 4.5 | 保存并通过 | 保存 + approve |
| 4.6 | 无横向滚动条 | 长内容自动换行 |

## 五、仪表盘（admin）

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 5.1 | 已扫描 | 来自 TaskLog，包含无命中的扫描 |
| 5.2 | 命中率 | feeds/scanned（插入待审池数/总扫描数） |
| 5.3 | 草稿 | 今日创建的所有 feed 数 |
| 5.4 | 已发布 | posted/approved 格式 |
| 5.5 | 新趋势 | 趋势生成的 feedsGenerated 数 |
| 5.6 | 系统健康 | 4 个服务状态卡片 |

## 六、用户管理（admin）

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 6.1 | 创建用户 | 成功后弹出凭据弹窗 |
| 6.2 | 用户名/邮箱查重 | 重复时 409 错误提示 |
| 6.3 | 重置密码 | 生成新密码，弹出凭据弹窗 |
| 6.4 | 角色修改 | 下拉切换角色 |
| 6.5 | 删除用户 | 确认弹窗后删除 |
| 6.6 | 浏览器不自动填充 | 新增时不填入当前用户信息 |

## 七、人设管理

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 7.1 | 密码保存 | 输入密码后保存，再编辑显示"已设定密码" |
| 7.2 | 验证登录 | 调用 BK API 验证账号密码 |
| 7.3 | 浏览器不自动填充密码 | autocomplete=off |

## 八、BK Forum API

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 8.1 | 登录 | mod/op/app/ver 在 query string，username/password 在 body |
| 8.2 | 发新帖 | 同上拆分，subject/message 在 body |
| 8.3 | 回帖 | 同上拆分，tid/fid/message 在 body |
| 8.4 | 发布失败重试 | 频率限制时 32s 重试 |

## 九、侧边栏

| # | 测试项 | 预期结果 |
|---|--------|---------|
| 9.1 | 审阅者统计 | 显示通过/已发布/拒绝/跳过 |
| 9.2 | 管理员不显示统计 | 只显示菜单 |
| 9.3 | 已认领数量 | 管理员看全局，审阅者看自己 |
| 9.4 | 操作后实时更新 | 认领/通过/拒绝/跳过/结束审核后立即刷新 |
